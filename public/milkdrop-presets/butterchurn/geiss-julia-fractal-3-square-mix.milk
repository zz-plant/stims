[preset00]
fRating=0.000000
fVideoEchoZoom=1.010000
fVideoEchoAlpha=0.500000
nVideoEchoOrientation=3.000000
nWaveMode=2.000000
bWaveThick=1.000000
fWaveAlpha=7.740000
fWaveScale=0.656000
fWaveSmoothing=0.800000
fWarpScale=1.772000
fZoomExponent=1.960000
fZoom=0.980000
fWarp=0.513000
fWaveR=0.500000
fWaveG=0.500000
fWaveB=0.500000
fOuterBorderSize=0.000000
fOuterBorderA=0.500000
fInnerBorderSize=0.070000
fInnerBorderA=0.025000
fMotionVectorsA=0.000000
wavecode_0_enabled=0.000000
wavecode_1_enabled=0.000000
wavecode_2_enabled=0.000000
wavecode_3_enabled=0.000000
shapecode_0_enabled=0.000000
shapecode_1_enabled=0.000000
shapecode_2_enabled=0.000000
shapecode_3_enabled=0.000000
per_frame_init_1=blah = 0;
per_frame_1=wave_x = wave_x + 0.5 * (0.6 * sin(2.121 * time) + 0.4 * sin(1.621 * time));
per_frame_2=wave_y = wave_y + 0.5 * (0.6 * sin(1.742 * time) + 0.4 * sin(2.322 * time));
per_frame_3=wave_r = wave_r + 0.5 * (0.6 * sin(0.823 * time) + 0.4 * sin(0.916 * time));
per_frame_4=wave_g = wave_g + 0.5 * (0.6 * sin(0.9 * time) + 0.4 * sin(1.023 * time));
per_frame_5=wave_b = wave_b + 0.5 * (0.6 * sin(0.808 * time) + 0.4 * sin(0.949 * time));
per_frame_6=zoom = zoom + 0.07 * (0.6 * sin(0.239 * time) + 0.4 * sin(0.296 * time));
per_frame_7=rot = rot + 0.038 * (0.6 * sin(0.381 * time) + 0.4 * sin(0.539 * time));
per_frame_8=cx = cx + 0.03 * (0.6 * sin(0.374 * time) + 0.4 * sin(0.194 * time));
per_frame_9=cy = cy + 0.037 * (0.6 * sin(0.274 * time) + 0.4 * sin(0.394 * time));
per_frame_10=dx = dx + 0.025 * (0.6 * sin(0.334 * time) + 0.4 * sin(0.277 * time));
per_frame_11=dy = dy + 0.025 * (0.6 * sin(0.384 * time) + 0.4 * sin(0.247 * time));
per_frame_12=sx = sx + 0.015 * (0.6 * sin(0.313 * time) + 0.4 * sin(0.383 * time));
per_frame_13=decay = decay - 0.01 * equal(mod(frame, 50), 0);
per_frame_14=ib_r = ib_r + 0.2 * sin(0.5413 * time);
per_frame_15=ib_g = ib_g + 0.2 * sin(0.3459 * time);
per_frame_16=ib_b = ib_b + 0.2 * sin(0.4354 * time);
per_frame_17=blah = 3 / (ib_r + ib_g + ib_b);
per_frame_18=ib_r = ib_r * blah;
per_frame_19=ib_g = ib_g * blah;
per_frame_20=ib_b = ib_b * blah;
per_pixel_1=rot = rot + 0.25 * rad * sin(0.3986 * time);

[warp_shader]
shader_body { 
  vec4 tmpvar_1;
  tmpvar_1 = texture (sampler_main, uv);
  vec4 tmpvar_2;
  tmpvar_2.w = 1.0;
  tmpvar_2.xyz = ((max (tmpvar_1.xyz, texture (sampler_main, 
    (uv + (vec2(0.0, 16.0) * ((tmpvar_1.xy - 0.5) * texsize.zw)))
  ).xyz) - 0.008) * 0.96);
  ret = tmpvar_2.xyz;
 }

[comp_shader]
shader_body { 
  vec2 tmpvar_1;
  tmpvar_1.x = cos((uv.x * 37.0));
  tmpvar_1.y = sin((uv.y * 29.0));
  vec4 tmpvar_2;
  tmpvar_2.w = 1.0;
  tmpvar_2.xyz = max (texture (sampler_main, uv).xyz, texture (sampler_main, (uv + (0.045 * tmpvar_1))).xyz);
  ret = tmpvar_2.xyz;
 }
