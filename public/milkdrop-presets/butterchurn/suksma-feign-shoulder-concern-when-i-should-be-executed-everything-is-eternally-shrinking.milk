[preset00]
fRating=4.000000
fGammaAdj=1.000000
fDecay=0.000000
fVideoEchoZoom=1.000000
nVideoEchoOrientation=3.000000
nWaveMode=7.000000
bAdditiveWaves=1.000000
bModWaveAlphaByVolume=1.000000
bMaximizeWaveColor=0.000000
bTexWrap=0.000000
fWaveAlpha=0.001000
fWaveScale=1.286000
fWaveSmoothing=0.630000
fModWaveAlphaStart=0.710000
fModWaveAlphaEnd=1.300000
fWarpScale=1.331000
fZoomExponent=0.699720
fZoom=0.994550
fWarp=0.010000
fWaveR=0.650000
fWaveG=0.650000
fWaveB=0.650000
fOuterBorderSize=0.000000
fOuterBorderR=0.010000
fOuterBorderA=1.000000
fInnerBorderSize=0.000000
fInnerBorderR=0.400000
fInnerBorderG=0.400000
fInnerBorderB=0.600000
fInnerBorderA=1.000000
nMotionVectorsX=64.000000
nMotionVectorsY=48.000000
nMotionVectorsLoop=0.000000
fMotionVectorsA=0.000000
wavecode_0_enabled=1.000000
wavecode_0_thick=1.000000
wavecode_1_enabled=1.000000
wavecode_1_thick=1.000000
wavecode_2_enabled=1.000000
wavecode_2_usedots=1.000000
wavecode_2_thick=1.000000
wavecode_3_enabled=1.000000
wavecode_3_thick=1.000000
shapecode_0_enabled=0.000000
shapecode_1_enabled=0.000000
shapecode_2_enabled=0.000000
shapecode_3_enabled=0.000000
per_frame_init_1=sim = 0;
per_frame_1=wave_y = wave_y + 0.15 * (0.6 * sin(1.742 * bass) + 0.35 * sin(1.921 * time));
per_frame_2=wave_x = wave_x + 0.15 * (0.6 * sin(2.111 * time) + 0.35 * sin(1.111 * time));
per_frame_3=wave_mystery = wave_mystery + 0.05 * sin(5.1 * time) + 0.04 * sin(4.5 * time);
per_frame_4=wave_r = wave_r + 0.5 * sin(1.11 * time);
per_frame_5=wave_b = wave_b + 0.5 * sin(1.3 * time);
per_frame_6=wave_g = wave_g + 0.5 * sin(1.2 * time);
per_frame_7=dx = dx + 0.01 * sin(5 * time);
per_frame_8=dy = dy + 0.01 * sin(10 * time);
per_frame_9=zoom = zoom - 0.05 * sin(0.2 * mid);
per_frame_10=rot = rot + 0.005 * sin(bass);
per_frame_11=cx = cx + 0.25 * sin(0.5 * bass);
per_frame_12=cy = cy + 0.25 * cos(0.5 * treb);
per_frame_13=dx = dx + 0.01 * sin(0.25 * time);
per_frame_14=dy = dy + 0.01 * sin(0.25 * time);
per_frame_15=wave_mystery = wave_mystery + 1 * above(sin(mod(frame, 3)), 0);
per_pixel_1=sim = 1.5 * sin(1.5 * bass);
per_pixel_2=zoom = zoom - if(0.00001 < abs(above(sim, 0.75)), 0.2 * (1 - rad), 0);
wave_0_init1=ma = 0;
wave_0_init2=mx = 0;
wave_0_init3=my = 0;
wave_0_init4=pi23 = 0;
wave_0_init5=vol = 0;
wave_0_per_point1=ma = ma + 3.1415 * above(bass, 1) * 0.01 * bass;
wave_0_per_point2=ma = ma - 3.1415 * above(treb, 1) * 0.01 * treb;
wave_0_per_point3=mx = mx + 0.0002 * cos(ma);
wave_0_per_point4=my = my + 0.0002 * sin(ma);
wave_0_per_point5=mx = if(0.00001 < abs(above(mx, 0.9)), 0.9 - mx, mx);
wave_0_per_point6=my = if(0.00001 < abs(above(my, 0.9)), 0.9 - my, my);
wave_0_per_point7=mx = if(0.00001 < abs(below(mx, 0.1)), 0.9 + mx, mx);
wave_0_per_point8=my = if(0.00001 < abs(below(my, 0.1)), 0.9 + my, my);
wave_0_per_point9=x = mx;
wave_0_per_point10=y = my;
wave_0_per_point11=a = above(bass + mid + treb, 0.8);
wave_0_per_point12=pi23 = 1.333333332 * asin(1);
wave_0_per_point13=vol = bass + mid + treb;
wave_0_per_point14=r = 0.5 * (sin(0.3 * time + vol - 0 * pi23) + 1);
wave_0_per_point15=g = 0.5 * (sin(0.3 * time + vol - 1 * pi23) + 1);
wave_0_per_point16=b = 0.5 * (sin(0.3 * time + vol - 2 * pi23) + 1);
wave_1_init1=ma = 0;
wave_1_init2=mx = 0;
wave_1_init3=my = 0;
wave_1_init4=pi23 = 0;
wave_1_init5=vol = 0;
wave_1_per_point1=ma = ma + 3.1415 * above(bass, 1) * 0.05 * bass;
wave_1_per_point2=ma = ma - 3.1415 * above(mid, 1) * 0.05 * mid;
wave_1_per_point3=mx = mx + 0.0001 * cos(ma);
wave_1_per_point4=my = my + 0.0001 * sin(ma);
wave_1_per_point5=mx = if(0.00001 < abs(above(mx, 0.9)), 0.9 - mx, mx);
wave_1_per_point6=my = if(0.00001 < abs(above(my, 0.9)), 0.9 - my, my);
wave_1_per_point7=mx = if(0.00001 < abs(below(mx, 0.1)), 0.9 + mx, mx);
wave_1_per_point8=my = if(0.00001 < abs(below(my, 0.1)), 0.9 + my, my);
wave_1_per_point9=x = mx;
wave_1_per_point10=y = my;
wave_1_per_point11=a = above(bass + mid + treb, 0.1);
wave_1_per_point12=pi23 = 1.333333332 * asin(1);
wave_1_per_point13=vol = bass + mid + treb;
wave_1_per_point14=r = 0.5 * (sin(0.3 * time + vol - 0 * pi23) + 1);
wave_1_per_point15=g = 0.5 * (sin(0.3 * time + vol - 1 * pi23) + 1);
wave_1_per_point16=b = 0.5 * (sin(0.3 * time + vol - 2 * pi23) + 1);
wave_2_init1=ma = 0;
wave_2_init2=mx = 0;
wave_2_init3=my = 0;
wave_2_init4=pi23 = 0;
wave_2_init5=vol = 0;
wave_2_per_point1=ma = ma + 3.1415 * above(mid, 1) * 0.01 * mid;
wave_2_per_point2=ma = ma - 3.1415 * above(treb, 1) * 0.01 * treb;
wave_2_per_point3=mx = mx + 0.0004 * cos(ma);
wave_2_per_point4=my = my + 0.0004 * sin(ma);
wave_2_per_point5=mx = if(0.00001 < abs(above(mx, 0.9)), 0.9 - mx, mx);
wave_2_per_point6=my = if(0.00001 < abs(above(my, 0.9)), 0.9 - my, my);
wave_2_per_point7=mx = if(0.00001 < abs(below(mx, 0.1)), 0.9 + mx, mx);
wave_2_per_point8=my = if(0.00001 < abs(below(my, 0.1)), 0.9 + my, my);
wave_2_per_point9=x = mx;
wave_2_per_point10=y = my;
wave_2_per_point11=a = above(bass + mid + treb, 0.3);
wave_2_per_point12=pi23 = 1.333333332 * asin(1);
wave_2_per_point13=vol = bass + mid + treb;
wave_2_per_point14=r = 0.5 * (sin(0.3 * time + vol - 0 * pi23) + 1);
wave_2_per_point15=g = 0.5 * (sin(0.3 * time + vol - 1 * pi23) + 1);
wave_2_per_point16=b = 0.5 * (sin(0.3 * time + vol - 2 * pi23) + 1);
wave_3_init1=ma = 0;
wave_3_init2=mx = 0;
wave_3_init3=my = 0;
wave_3_init4=pi23 = 0;
wave_3_init5=vol = 0;
wave_3_per_point1=ma = ma + 3.1415 * above(bass, 0.5) * 0.02 * bass;
wave_3_per_point2=ma = ma - 3.1415 * above(treb, 0.5) * 0.02 * treb;
wave_3_per_point3=mx = mx + 0.0008 * cos(ma);
wave_3_per_point4=my = my + 0.0008 * sin(ma);
wave_3_per_point5=mx = if(0.00001 < abs(above(mx, 0.9)), 0.9 - mx, mx);
wave_3_per_point6=my = if(0.00001 < abs(above(my, 0.9)), 0.9 - my, my);
wave_3_per_point7=mx = if(0.00001 < abs(below(mx, 0.1)), 0.9 + mx, mx);
wave_3_per_point8=my = if(0.00001 < abs(below(my, 0.1)), 0.9 + my, my);
wave_3_per_point9=x = mx;
wave_3_per_point10=y = my;
wave_3_per_point11=a = above(bass + mid + treb, 0.2);
wave_3_per_point12=pi23 = 1.333333332 * asin(1);
wave_3_per_point13=vol = bass + mid + treb;
wave_3_per_point14=r = 0.5 * (sin(0.3 * time + vol - 0 * pi23) + 1);
wave_3_per_point15=g = 0.5 * (sin(0.3 * time + vol - 1 * pi23) + 1);
wave_3_per_point16=b = 0.5 * (sin(0.3 * time + vol - 2 * pi23) + 1);

[warp_shader]
shader_body { 
  vec2 tmpvar_1;
  tmpvar_1 = (((uv_orig * texsize.xy) * (texsize_noise_lq.zw * 1.5)) + rand_frame.xy);
  vec4 tmpvar_2;
  tmpvar_2.w = 1.0;
  tmpvar_2.xyz = ((texture (sampler_main, (
    mix (uv_orig, uv, vec2(((dot (texture (sampler_main, 
      mix (uv_orig, uv, vec2(4.0, 4.0))
    ).xyz, vec3(0.32, 0.49, 0.29)) - dot (
      ((texture (sampler_blur2, mix (uv_orig, uv, vec2(-12.0, -12.0))).xyz * scale2) + bias2)
    , vec3(0.32, 0.49, 0.29))) * 12.0)))
   + 
    (((texture (sampler_noise_lq, tmpvar_1) - 0.5).xy * texsize.zw) * 0.5)
  )).xyz + (
    (texture (sampler_noise_lq, tmpvar_1) - 0.5)
   * 0.006).xyz) + -0.0006);
  ret = tmpvar_2.xyz;
 }

[comp_shader]
shader_body { 
  vec2 uv_1;
  vec3 ret_2;
  uv_1 = (0.05 + (0.9 * uv));
  vec2 tmpvar_3;
  tmpvar_3 = (((uv_1 - 0.5) * vec2(-1.0, 1.0)) + 0.5);
  ret_2 = (abs((
    ((texture (sampler_blur1, uv_1).xyz * scale1) + bias1)
   - 
    mix (texture (sampler_main, uv_1).xyz, texture (sampler_main, tmpvar_3).xyz, vec3(0.5, 0.5, 0.5))
  )) * 6.0);
  ret_2 = (ret_2 * (0.1 + abs(
    max (sin(bass_att), sin(treb_att))
  )));
  ret_2 = (ret_2 * max ((
    (texture (sampler_blur3, uv_1).xyz * scale3)
   + bias3), (
    (texture (sampler_blur3, tmpvar_3).xyz * scale3)
   + bias3)));
  ret_2 = (ret_2 * 1.5);
  vec4 tmpvar_4;
  tmpvar_4.w = 1.0;
  tmpvar_4.xyz = ret_2;
  ret = tmpvar_4.xyz;
 }
