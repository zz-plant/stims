// src/viewport-config.ts
/** Centralised viewport defaults for the Stims project */
export const DEFAULT_VIEWPORT = {
  /** Width in CSS pixels used when a script does not specify a viewport width */
  width: 1280,
  /** Height in CSS pixels used when a script does not specify a viewport height */
  height: 720,
};
