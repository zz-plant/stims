import { createMilkdropPresetToyStarter } from './milkdrop-preset-toy';

export const start = createMilkdropPresetToyStarter({
  presetId: 'symph',
  title: 'Spectrograph',
  description:
    'A spectrograph-inspired preset with calm bands, soft trails, and readable music response.',
});
