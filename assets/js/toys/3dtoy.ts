import { createMilkdropPresetToyStarter } from './milkdrop-preset-toy';

export const start = createMilkdropPresetToyStarter({
  presetId: '3dtoy',
  title: '3D Toy',
  description:
    'A twisting MilkDrop tunnel preset with chrome hues, orbiting mesh, and audio-driven spin.',
});
