import { createMilkdropPresetToyStarter } from './milkdrop-preset-toy';

export const start = createMilkdropPresetToyStarter({
  presetId: 'mobile-ripples',
  title: 'Mobile Ripples',
  description:
    'A low-motion ripple preset with neon rings, calm drift, and lightweight audio response.',
});
