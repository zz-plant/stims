import { createMilkdropPresetToyStarter } from './milkdrop-preset-toy';

export const start = createMilkdropPresetToyStarter({
  presetId: 'battle-fan',
  title: 'Battle Fan',
  description:
    'A radial fan preset with spoke-like bursts, kick-snare flips, and fast reactive color changes.',
});
