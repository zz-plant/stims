import { createMilkdropPresetToyStarter } from './milkdrop-preset-toy';

export const start = createMilkdropPresetToyStarter({
  presetId: 'multi',
  title: 'Multi-Capability Visualizer',
  description:
    'A hybrid preset that folds the old multi-capability vibe into one dense, reactive MilkDrop scene.',
});
